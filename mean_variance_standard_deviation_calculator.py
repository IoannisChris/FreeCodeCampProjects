# -*- coding: utf-8 -*-
"""Mean-Variance-Standard Deviation Calculator.ipynb

Automatically generated by Colab.

Original file is located at
    https://colab.research.google.com/drive/1uxjiS-zs0yngj71rfTI7KVPB1SYzJpfU
"""

import numpy as np

def calculate(list):
    # 1. The Error Check
    # freeCodeCamp requires exactly 9 numbers to form a 3x3 grid.
    if len(list) != 9:
        raise ValueError("List must contain nine numbers.")

    # 2. Reshape the list into a 3x3 Numpy matrix
    ls = np.array(list).reshape((3, 3))

    # 3. Calculate all the required metrics
    # axis=0 means columns, axis=1 means rows, and leaving it blank means the flattened matrix
    calculations = {
        'mean': [ls.mean(axis=0).tolist(), ls.mean(axis=1).tolist(), ls.mean().tolist()],
        'variance': [ls.var(axis=0).tolist(), ls.var(axis=1).tolist(), ls.var().tolist()],
        'standard deviation': [ls.std(axis=0).tolist(), ls.std(axis=1).tolist(), ls.std().tolist()],
        'max': [ls.max(axis=0).tolist(), ls.max(axis=1).tolist(), ls.max().tolist()],
        'min': [ls.min(axis=0).tolist(), ls.min(axis=1).tolist(), ls.min().tolist()],
        'sum': [ls.sum(axis=0).tolist(), ls.sum(axis=1).tolist(), ls.sum().tolist()]
    }

    return calculations